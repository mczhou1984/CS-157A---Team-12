const config = {
  host: 'localhost',
  user: 'root',
  password:'password',
  database: 'budgeteddb',
  secret: 'yoursecret',
  multipleStatements: true,
  dateStrings: 'date'
};

module.exports = config;
